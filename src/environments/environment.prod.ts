import { common } from './common';

export const environment = {
  ...common,
  production: true,
  desarollo: false,
  preproduccion: false,
  local: true,
  name: 'PRODUCCIÓN',
  webUrl: 'https://juamber.com',
};
